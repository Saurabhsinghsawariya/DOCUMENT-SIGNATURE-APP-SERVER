services:
  - type: web
    name: document-signature-app-server
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        fromDatabase: false
      - key: MONGO_URI
        fromDatabase: false
      - key: CLIENT_URL
        fromDatabase: false
